# Sanitized Engineering Memory

这是一份人工精选的可迁移记忆，不是原始本机 MEMORY 的逐字脱敏副本。项目名、客户名、产品代号、精确路径、会话 ID、设备 ID、串口、网络地址、源码细节和硬件测量值均未保留。

## Working profile

- 主要环境是 Windows，常见任务包括嵌入式固件、本地 Codex/MCP/Skill 基础设施和工程工具维护。
- 回复默认结论优先、简洁、可执行；复杂结论应有真实代码、日志、构建或硬件证据支撑。
- 采用最小、可逆、与现有项目兼容的实现；不要把狭窄修复扩大成无关重构。
- 当普通缺点不阻塞任务时只提醒一次；当存在数据丢失、硬件损坏、账号/权限或不可逆风险时必须暂停确认。
- 不替用户虚构目标，不为迎合偏好而隐藏反证。

## Embedded engineering gates

- 嵌入式任务先路由到最小匹配工作流，再加载证据验证规则。
- 调试按层次推进：构建、烧录、启动日志、外设初始化、协议握手、业务状态机。
- 明确区分“构建通过”“烧录成功”“启动正常”“真实硬件行为验证通过”；前一项不能替代后一项。
- 优先做单变量 A/B 和最短因果链，日志、寄存器读数、仪器测量和可复现现象优先于猜测。
- 正式发布或归档时枚举所有真实应用、Bootloader 和设备侧目标；每个目标必须产生并核验所需格式，失败必须显式报告。
- 修改前保存工作树状态；正式构建优先在隔离快照中完成，不清理或重置用户 checkout。

## Privacy and sharing defaults

- 对外共享默认采用最小暴露：源码仓库或脱敏压缩包优于实时控制面；元数据优于密钥、日志和数据库。
- 任何 `auth`、`token`、OAuth lock、浏览器状态、session/history、SQLite、真实设备配置和本机路径映射默认不提交。
- 分享 MCP 时，服务源码、运行配置和持久数据必须分离；仓库只携带源码与占位配置。
- 私有仓库不是秘密管理器；提交前仍要运行敏感信息扫描，并从全新 Git 历史开始。
- 第三方组件先检查许可证、来源、依赖、遥测/上传、自动更新和维护状态；没有许可证不等于允许再分发。

## Codex and Skill conventions

- 全局 Codex/MCP/hooks/config 经验放全局记忆；仓库事实留在项目级上下文，避免交叉污染。
- Skill 入口描述只保留触发条件和边界，具体步骤放在 Skill 正文与 references/scripts 中。
- Windows 上的 Skill 可能通过 Junction/SymbolicLink 指向 canonical 目录；修改或备份前先解析真实目标并去重。
- Skill 验证遇到本地编码问题时使用 UTF-8 模式；不要为了通过验证而改动无关内容。
- 系统 Skill 和插件缓存由其安装渠道恢复，通常只记录名称和版本，不把缓存复制进个人仓库。

## Reusable MCP failure shields

- STDIO MCP 在 `initialize` 前断开，通常表示子进程已退出；先看 stderr、解释器路径和入口文件，不要先归因于业务设备。
- Python 解释器移动后，MCP 配置和 hook wrapper 可能仍指向旧路径；修正后要重新验证配置并重启 Codex/新建会话以刷新工具 schema。
- MCP schema 变化不一定会在当前会话热刷新；工具缺失时先区分“服务未启动”和“会话未刷新”。
- `list_mcp_resources` 为空只说明没有枚举型 resources/templates，不代表 MCP tools 没有挂载。
- 启动超时与服务逻辑失败要分开诊断；先测 MCP initialize，再调整合理的启动预算。
- 远程 OAuth MCP 只保存端点模板；认证必须在目标机器重新交互完成。

## Visual and document quality

- 文档、PDF、演示和可视化交付需要检查最终渲染页面，而不是只检查源文件。
- UI 工作要同时审查层级、间距、字体、颜色、可访问性和响应式状态，不能只保证“能运行”。
- EDA 原理图和 PCB 的结构正确不等于工程质量通过；功能分区、网络标注、文字/器件避让、布局和 DRC 都是独立门槛。

## Memory hygiene

- 长期记忆保存可复用的结论、失败模式、验证步骤与边界，不保存密钥、客户资料、序列号、内网地址或敏感源码。
- 旧记忆可能过时；路径、工具版本、远程服务、市场/规则和硬件状态应在当前任务中低成本复核。
- 上下文压缩后重新读取磁盘上的有效指令链，核对当前请求、已完成工作、剩余步骤和风险后继续，不从头重做。

